<p style="color: green"><%= notice %></p>

<%= render @user %>

<div>
  <%= link_to t("users.edit"), edit_user_path(@user) %> |
  <%= link_to t("users.back"), users_path %>

  <%= button_to t("users.destroy"), @user, method: :delete %>
</div>
